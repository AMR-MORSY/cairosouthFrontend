import { BackNavigateDirective } from './back-navigate.directive';

describe('BackNavigateDirective', () => {
  it('should create an instance', () => {
    const directive = new BackNavigateDirective();
    expect(directive).toBeTruthy();
  });
});
